/* ORDER BY is used to sort the data, either on Ascending or decending order*/

select * from customers order by age desc;

select * from customers order by age asc;
 
select * from customers order by name asc;